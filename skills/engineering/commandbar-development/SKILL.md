---
name: commandbar-development
description: Build, debug, or explain Microsoft PowerToys Command Palette extensions in C#/.NET. Use when working on Windows Command Palette extension development, command providers, list/detail/form/markdown/grid pages, invokable commands, fallback commands, extension manifests, local deployment/reload/debugging, packaging, publishing, or the Microsoft.CommandPalette.Extensions SDK/toolkit.
---

# CommandBar Development

## Quick Orientation

Treat "CommandBar" as the user's shorthand for Microsoft PowerToys Command Palette extension development.

Prefer official Microsoft Learn and PowerToys GitHub sources. The docs are still evolving, so browse the official docs when exact API names, packaging rules, or publishing steps matter.

Read `references/docs-map.md` when you need doc links, the current conceptual model, common gotchas, or a refresher on project structure.

## Default Workflow

1. Identify whether the task is about scaffold/setup, commands/pages, debugging, packaging/publishing, or API lookup.
2. Inspect the local repo before suggesting changes. Command Palette extension projects are usually C#/.NET Windows App SDK projects generated by the Command Palette template.
3. Use the template's existing structure and namespaces. Avoid inventing a different architecture unless the user asks for a redesign.
4. For runtime behavior, make changes in the command provider, page classes, and command classes rather than manifest/package files unless discovery, deployment, or publishing is the issue.
5. After code changes, prefer building with the repo's solution/project commands, then remind that Visual Studio deploy plus Command Palette `Reload Command Palette Extension` may be required for the installed package to refresh.

## Common Implementation Targets

- Extension discovery: inspect `Package.appxmanifest`, COM server registration, app extension declaration, CLSIDs, and package deployment state.
- Top-level entry points: inspect the extension's `IExtension` implementation and `ICommandProvider`.
- List content: update the relevant `ListPage` and its `GetItems()` method.
- Simple actions: use toolkit commands such as `OpenUrlCommand` or `CopyTextCommand` where appropriate.
- Custom actions: implement `InvokableCommand`/`IInvokableCommand` classes and return an appropriate `CommandResult`.
- Icons: prefer toolkit `IconInfo` and `IconHelpers.FromRelativePath(...)` for local assets.
- Debugging: use Debug configuration, Visual Studio debug output, and explicit reloads from Command Palette after redeploying.

## Practical Gotchas

- Building is not the same as deploying. The package must be deployed for Command Palette to discover or update the extension.
- Running the unpackaged target from Visual Studio does not deploy the package.
- Command Palette may not notice a changed package until the `Reload Command Palette Extension` command is run.
- Keep `Properties/launchSettings.json` and `*.pubxml` files when the project relies on Windows App SDK deployment profiles; a default C# `.gitignore` may exclude them.
- Manifest changes are fragile. If editing manually, keep the COM class GUID consistent across the COM server declaration, app extension activation, and generated code.

## Reference Loading

Open `references/docs-map.md` for:

- official documentation links
- API namespace links
- sample links
- current project structure notes
- deployment and reload reminders

Browse the official docs again when the user asks for the latest guidance, exact API signatures, or publishing requirements.
